---
layout: base.njk
title: EasyHTPC - Your Guide to Home Theater PCs & Media Centers
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">{{ site.tagline }}</h1>
      <p class="hero-subtitle">Expert guides, reviews, and tutorials for building your ultimate home media experience. From streaming to storage, we've got you covered.</p>
    </div>
  </div>
</section>

<section class="categories-section">
  <div class="container">
    <h2 class="section-title">Browse by Category</h2>
    <div class="categories-grid">
      {% for cat in categories %}
      <a href="/categories/{{ cat.slug }}/" class="category-card" style="--cat-color: {{ cat.color }}">
        <span class="category-icon">{{ cat.icon }}</span>
        <span class="category-name">{{ cat.name }}</span>
        <span class="category-desc">{{ cat.description | truncate(80) }}</span>
      </a>
      {% endfor %}
    </div>
  </div>
</section>

<section class="articles-section">
  <div class="container">
    <h2 class="section-title">Latest Articles</h2>
    <div class="articles-grid">
      {% for post in collections.posts | limit(6) %}
      <article class="article-card">
        <div class="article-image">
          <img src="{{ post.data.image | default('https://images.unsplash.com/photo-1593359677879-a4bb92f829d1?w=800&h=400&fit=crop') }}" alt="{{ post.data.title }}" loading="lazy">
        </div>
        <div class="article-content">
          <div class="article-meta">
            {% if post.data.category %}
            <span class="article-category">{{ post.data.category }}</span>
            {% endif %}
            <span class="article-date">{{ post.date | dateFormat }}</span>
          </div>
          <h3 class="article-title">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
          </h3>
          <p class="article-excerpt">{{ post.data.description | default(post.templateContent | excerpt(150)) }}</p>
          <span class="article-read-more">
            Read More
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
          </span>
        </div>
      </article>
      {% endfor %}
    </div>
    <div class="articles-cta">
      <a href="/articles/" class="btn btn-primary">View All Articles</a>
    </div>
  </div>
</section>